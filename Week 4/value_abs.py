# Program that prompts the user to enter an integer and displays 
# the absolute value of that integer on one line and then the original integer on the next line

integer = (input('Enter an integer >'))
print(abs(int(integer)))
print(integer)