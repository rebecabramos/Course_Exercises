# Program that prompts the user to enter an integer and displays that integer.
# The program should then prompt the user to enter the integer again and display its binary representation

print(round(float(input('Enter an integer >'))))
print(bin(int(input('Enter it again >'))))